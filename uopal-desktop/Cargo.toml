[package]
name = "uopal-desktop"
description = "A library to query and put information about the user desktop, such as themes, installed APPs and such"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
